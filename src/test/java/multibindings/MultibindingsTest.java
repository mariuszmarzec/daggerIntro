package multibindings;

import org.junit.Test;

import static org.junit.Assert.assertEquals;
import static org.junit.Assert.assertTrue;

public class MultibindingsTest {

    @Test
    public void multibindingsTest() {
//        ParentComponent parentComponent = DaggerParentComponent.create();
//        ChildComponent childComponent = parentComponent.buildChildComponent();
//
//        assertTrue(parentComponent.getMap().isEmpty());
//        assertEquals(1, childComponent.getMap().get("1").intValue());
//        assertEquals(2, childComponent.getMap().get("2").intValue());
    }
}
